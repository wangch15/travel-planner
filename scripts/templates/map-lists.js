// 每日的 Google Maps 私人清單。改這個檔之前先讀 docs/schema/map-lists.md。
// trip.config 的 sections.mapLists 為 false 時不需要填。

module.exports = {
  // 1: { name: 'Day 1', url: 'https://maps.app.goo.gl/…', placeKeys: [] },
};
