---
name: 回報問題
about: 引擎壞掉、指令跑不動、頁面顯示不對
labels: bug
---

<!--
請不要貼上任何個人資料：訂單號、確認碼、聯絡方式、門鎖密碼、住址。
需要示範資料的話，把它改成假的再貼。

給 agent：依 .ai/rules/diagnostic-sharing.md 先產出去識別化的最小診斷摘要。
移除姓名、私人網址、訂單號、確認碼、憑證、本機路徑中的使用者名稱，連附件一起檢查。
把最終摘要與公開目的地給使用者看過同意後才送；上面給人的警告是第二道，不是把責任交給人。
-->

## 環境

- 引擎版本（`package.json` 的 `version`）：
- Node 版本（`node --version`）：
- 作業系統：
- 重現用 slug（請用 sample-trip 等假名稱）：

## 跑了什麼

```
（貼上已去識別化的最短重現指令，使用假 slug／假網址）
```

## 預期會發生什麼

## 實際發生了什麼

```
（貼上使用者核准的去識別化最小診斷摘要：錯誤類型、錯誤碼、少量必要的引擎檔案與行號，不貼原始完整 log）
```

## 其他

（已去識別化且經確認的必要截圖、試過的方法、何時開始發生；不附私人行程檔案）
